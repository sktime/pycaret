[project]
name = "pycaret"
version = "3.4.0"
description = "PyCaret - An open source, low-code machine learning library in Python."
readme = "README.md"
keywords = [
  "data-mining",
  "data-science",
  "classification",
  "clustering",
  "detection",
  "forecasting",
  "machine-learning",
  "regression",
  "scikit-learn",
  "time-series",
]
license = { file = "LICENSE" }
maintainers = [
  { name = "pycaret developers" },
]
# for full credits see contributors.md
authors = [
    { name = "pycaret developers" },
]
requires-python = ">=3.9,<3.15"
classifiers = [
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS",
  "Operating System :: Microsoft :: Windows",
  "Operating System :: POSIX",
  "Operating System :: Unix",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Topic :: Software Development",
]
# core dependencies of pycaret
# this set should be kept minimal!
dependencies = [
  # Base
  "packaging",
  "scikit-base>=0.6.1",
  "ipython>=5.5.0",
  "ipywidgets>=7.6.5",
  "tqdm>=4.62.0",
  "numpy>=1.21,<3",  # numpy 2.x required for python 3.13+ wheels
  "pandas>=2.2.0",  # latest sktime makes tests fail if pandas 2.1
  "jinja2>=3",  # not imported directly, required by pandas Styler which pycaret uses
  "scipy>=1.6.1,<2",  # scipy 1.14.1+ required for python 3.13+ wheels
  "joblib>=1.2.0",  # joblib<1.2.0 is vulnerable to Arbitrary Code Execution; 1.4+ needed on python 3.14
  "scikit-learn>1.4.0",
  "pyod>=1.1.3",  # 1.1.3 to support scikit-learn 1.4
  "imbalanced-learn>=0.12.0",  # >=0.12.0 to support scikit-learn 1.4
  "category-encoders>=2.4.0",
  "lightgbm>=3.0.0",
  "requests>=2.27.1",  # Required by pycaret.datasets
  "psutil>=5.9.0",
  "cloudpickle",
  "xxhash",
  "setuptools; python_version>='3.12'",
  # Plotting
  "matplotlib<4",  # <3.8 cap lifted for py3.13+ wheels; known gap: yellowbrick 1.5 cooks plot still uses removed stem kwarg
  "mljar-scikit-plot",  # this is a updated version of original scikit-plot
  "yellowbrick>=1.4",
  "plotly>=5.14.0",
  "kaleido>=0.2.1",  # not imported directly, needed by plotly for the png renderer used on large TS plots
  "schemdraw==0.15",  # 0.16 only supports Python >3.8
  "plotly-resampler>=0.8.3.1",
  # Time-series
  "statsmodels>=0.12.1",
  "sktime",  # sktime adds support for scikit-learn 1.5 in version sktime 0.31
  "tbats>=1.1.3",
  "pmdarima>=2.0.4",
]

[project.optional-dependencies]
# there are the following dependency sets:
# - full - all soft dependencies
# - analysis
# - models
# - tuners
# - mlops
# - parallel
# - dev
# - test

# soft dependencies are not required for the core functionality of pycaret

# all soft dependencies
#
# users can install via "pip install pycaret[full]"
#
full = [
  "shap~=0.44.0",  # 0.44.0 fixes matplotlib use_line_collection error
  "interpret>=0.2.7",
  "umap-learn>=0.5.2",
  "pyyaml",
  "ydata-profiling>=4.3.1",
  "explainerdashboard>=0.3.8",  # For dashboard method
  "fairlearn==0.7.0",  # For check_fairness method
  "xgboost>=2.0.0",
  "catboost>=1.2.5",
  "kmodes>=0.11.1",
  "mlxtend>=0.19.0",
  "statsforecast<1.8.0,>1.6",
  "scikit-learn-intelex>=2023.0.1; platform_machine == 'x86_64' or platform_machine == 'AMD64'",
  "tune-sklearn",
  "ray[tune]",
  "hyperopt>=0.2.7",
  "optuna>=3.0.0",
  "optuna-integration",
  "scikit-optimize>=0.9.0",
  "mlflow>=2.0.0",
  "gradio>=3.50.2",
  "boto3>=1.24.56",  # For deploy_model method
  "fastapi",  # For web api
  "uvicorn>=0.17.6",  # For web api
  "m2cgen>=0.9.0",  # For model conversion
  "evidently<0.4.30",  # test_check_drift fails with 0.4.30
  "dask>=2024.4.1",  # Python 3.11.9 breaks Dask (
  "distributed>=2024.4.1",  # Python 3.11.9 breaks Dask (
  "fugue~=0.8.0",
  "flask",
  "Werkzeug>=2.2,<3.0",
  "pytest==8.3.2",
  "moto<5.0.0",  # missing moto.mock_s3 on moto>=5.0.0
  # "codecov>=2.1.12",  # https://github.com/home-assistant/core/issues/91283
  "fugue[dask]",
  "dash[testing]",
  "black>=24.8.0",
  "isort>=5.13.2",
  "flake8>=7.1.1",
  "setuptools>=71.1.0",
  "mypy>=0.11.1",
]

analysis = [
  "shap~=0.44.0",  # 0.44.0 fixes matplotlib use_line_collection error
  "interpret>=0.2.7",
  "umap-learn>=0.5.2",
  "pyyaml",
  "ydata-profiling>=4.3.1",
  "explainerdashboard>=0.3.8",  # For dashboard method
  "fairlearn==0.7.0",  # For check_fairness method
]

models = [
  "xgboost>=2.0.0",
  "catboost>=1.2.5",
  "kmodes>=0.11.1",
  "mlxtend>=0.19.0",
  "statsforecast<1.8.0,>1.6",
  "scikit-learn-intelex>=2023.0.1; platform_machine == 'x86_64' or platform_machine == 'AMD64'",
]

tuners = [
  "tune-sklearn",
  "ray[tune]",
  "hyperopt>=0.2.7",
  "optuna>=3.0.0",
  "optuna-integration",
  "scikit-optimize>=0.9.0",
]

mlops = [
  "mlflow>=2.0.0",
  "gradio>=3.50.2",
  "boto3>=1.24.56",  # For deploy_model method
  "fastapi",  # For web api
  "uvicorn>=0.17.6",  # For web api
  "m2cgen>=0.9.0",  # For model conversion
  "evidently<0.4.30",  # test_check_drift fails with 0.4.30
]

parallel = [
  "dask>=2024.4.1",  # Python 3.11.9 breaks Dask (
  "distributed>=2024.4.1",  # Python 3.11.9 breaks Dask (
  "fugue~=0.8.0",
  "flask",
  "Werkzeug>=2.2,<3.0",
]

prophet = [
  "prophet>=1.0.1",
]

# dev - the developer dependency set, for contributors to sktime
dev = [
  "black>=24.8.0",
  "isort>=5.13.2",
  "flake8>=7.1.1",
  "setuptools>=71.1.0",
  "mypy>=0.11.1",
]


# CI related soft dependency sets - not for users of pycaret
# they are stable and subject to deprecation policies
test = [
  "pytest==8.3.2",
  "moto<5.0.0",  # missing moto.mock_s3 on moto>=5.0.0
  # "codecov>=2.1.12",  # https://github.com/home-assistant/core/issues/91283
  "fugue[dask]",
  "dash[testing]",
  "pytest-timeout",
]


[project.urls]
"API Reference" = "https://pycaret.gitbook.io/"
Documentation = "https://pycaret.gitbook.io/"
Download = "https://pypi.org/project/pycaret/#files"
Homepage = "https://pycaret.org/"
"Release Notes" = "https://github.com/pycaret/pycaret/releases"
Repository = "https://github.com/pycaret/pycaret"

[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>61",
]

[tool.setuptools.packages.find]
exclude = ["tests", "tests.*"]
